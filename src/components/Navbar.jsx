import React from "react";
import Header from "./wazifa/Header";

/**
 * Navbar – used on all non-home routes.
 * Simply renders the full Header (TopBar + MainNavbar).
 */
const Navbar = () => <Header />;

export default Navbar;
